document.addEventListener('DOMContentLoaded', () => {
    console.log('dashboard.js loaded. Application ready!');


    const addMemoryButton = document.querySelector('a[href="add_memory.php"]');
    if (addMemoryButton) {
        addMemoryButton.addEventListener('click', (event) => {

        });
    }
});